## metalcloud-cli device-config-template list

List device configuration templates with optional filtering

```
metalcloud-cli device-config-template list [flags]
```

### Options

```
      --filter-id strings      Filter by template ID.
      --filter-label strings   Filter by template label.
      --filter-name strings    Filter by template name.
  -h, --help                   help for list
```

### Options inherited from parent commands

```
  -k, --api_key string         MetalCloud API key
  -c, --config string          Config file path
  -d, --debug                  Set to enable debug logging
  -e, --endpoint string        MetalCloud API endpoint
  -f, --format string          Output format. Supported values are 'text','csv','md','json','yaml'. (default "text")
  -i, --insecure_skip_verify   Set to allow insecure transport
  -l, --log_file string        Log file path
  -v, --verbosity string       Log level verbosity (default "INFO")
```

### SEE ALSO

* [metalcloud-cli device-config-template](metalcloud-cli_device-config-template.md)	 - Manage device configuration templates and profiles

